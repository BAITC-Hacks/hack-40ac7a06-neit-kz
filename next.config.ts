import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Служебный индикатор dev-сборки перекрывал угол на скриншотах для README.
  devIndicators: false,
};

export default nextConfig;
